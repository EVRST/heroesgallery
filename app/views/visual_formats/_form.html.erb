<%= simple_form_for(@visual_format) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :visual_id %>
    <%= f.input :format_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
