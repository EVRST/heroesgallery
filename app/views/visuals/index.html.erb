<div class="row artists">
<p id="notice"><%= notice %></p>

<div class="container container-content">
  <div class="col-md-4">
  Sélectionnez par
</div>
<div class="col-md-8">
  <div class="row">
    <% @visuals.each do |visual| %>
      <div class="col-md-4">
        <%= visual.artist.full_name %>
        <%= link_to image_tag("http://placehold.it/270x270"), visual %>
      </div>
    <% end %>
  </div>

</div>
<!--
<h1>Listing Visuals</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Price</th>
      <th>Artist</th>
      <th>Is drawing</th>
      <th>Is picture</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @visuals.each do |visual| %>
      <tr>
        <td><%= visual.name %></td>
        <td><%= visual.price %></td>
        <td><%= visual.artist_id %></td>
        <td><%= visual.is_drawing %></td>
        <td><%= visual.is_picture %></td>
        <td><%= link_to 'Show', visual %></td>
        <td><%= link_to 'Edit', edit_visual_path(visual) %></td>
        <td><%= link_to 'Destroy', visual, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Visual', new_visual_path %>-->
</div>
</div>