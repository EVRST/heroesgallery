<div class="row artists">

  <div class="container">

    <div class="row start-page">


      <div class="col-md-3">
        <div class="panel panel-default visual-cart">
          <div class="panel-body text-center">
            <h4>
              Format
              <small> <%= @visual.gabarits.last.name if @visual.gabarits.any? %></small>
            </h4>

         <%= render partial: "visuals/choose_dimensions", locals: {visual: @visual, order_item: @order_item} %>
            
          <hr>

            <h4>Finition</h4>
          <%= render partial: "visuals/chooser_finish", locals: {visual: @visual, order_item: @order_item} %>
          <hr>
            
            <!--<h4><%= @visual.price %> €</h4>
              <%= link_to "Ajouter au panier", "", class: "btn btn-primary btn-block" %>-->

              <%= render partial: "visuals/add_to_cart", locals: {visual: @visual, order_item: @order_item} %>
            
          </div>
        </div>
        <div class="social-likes-position">
          <div class="social-likes">
              <div data-service="facebook" title="Share link on Facebook"></div>
              <div data-service="twitter" title="Share link on Twitter"></div>
              <div data-service="plusone" title="Share link on Google+"></div>
              <div data-service="pinterest" title="Share link on Pinterest" data-media="image link, required"></div>

              <!-- <div data-service="vkontakte" title="Share link on Vkontakte">Vkontakte</div> -->
              <!-- <div data-service="odnoklassniki" title="Share link on Odnoklassniki">Odnoklassniki</div> -->
          </div>
        </div>
        
        
      </div>
      <div class="col-md-9">
        <div class="visual-right-part">
        <%= image_tag @visual.image.url(:large), class: "responsive-img" %>
      </div>
        <div class="row">
          <div class="col-md-9">
            <h3>
              <%= @visual.name %>
              <%= link_to "Modifier", edit_visual_path(@visual), class: "btn btn-primary btn-xs" %>
            </h3>
            <h4><%= link_to @visual.artist.full_name, artist_path(@visual.artist) %></h4>

          </div>
          <div class='col-md-3 text-right'>
            <br>
            <p class='lead'>à partir de <%= @visual.price %> €</p>
          </div>
        </div>
        <p><%= @visual.presentation_fr %></p>
      </div>
    </div>

    <% if @visual.artist.visuals.count > 1 %>
    <div class="other-visuals">
      <h1 class="text-center">Autres visuels de <%= @visual.artist.full_name %></h1>

      <%= render partial: "artists/artist_visuals", locals: {visuals: @visual.artist.visuals} %>

      
    </div>
    <% end %>




  </div>
</div>


